{
  "name": "manhole-japan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An interactive map of Japan's decorative manhole covers.",
  "scripts": {
    "lint": "eslint src/",
    "smoke": "node scripts/smoke.mjs",
    "serve": "python3 -m http.server 8777",
    "data": "python3 scripts/fetch_pokefuta.py && python3 scripts/build_covers.py && python3 scripts/validate.py",
    "images": "python3 scripts/optimize_images.py"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jsdom": "^25.0.0"
  }
}
